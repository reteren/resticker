# resticker — дорожная карта

Порядок вех выбран так, чтобы **каждая заканчивалась чем-то запускаемым**, и чтобы самые рискованные технические вещи проверялись как можно раньше, пока не поздно менять архитектуру.

Оценки — для одного разработчика, работающего с помощью Claude, при загрузке порядка 10–15 часов в неделю. Это ориентир, а не обещание. Реальность обычно оказывается в полтора раза дольше.

---

## Спайк S0 — проверка гипотезы (2–4 дня, **до всего остального**)

Самое главное в проекте — маска перекрытия и прозрачное композиционное окно. Если это не работает, вся архитектура другая. Поэтому это делается **первым**, одним грязным прототипом на 300 строк, который потом выбрасывается.

- [x] Win32-окно с `WS_EX_NOREDIRECTIONBITMAP | WS_EX_TOPMOST | WS_EX_TRANSPARENT`
- [x] D3D11 + DirectComposition, swapchain через `CreateSwapChainForComposition` с premultiplied alpha
- [x] Отрисовать один PNG с прозрачностью
- [x] Убедиться, что клики проходят насквозь
- [x] `SetWinEventHook` на `FOREGROUND` + `LOCATIONCHANGE`, вывод прямоугольников окон в консоль
- [x] Вырезать из PNG прямоугольник окна «Блокнот» через маску — и увидеть, что PNG действительно «уходит под» Блокнот
- [x] Замерить CPU при перетаскивании окна по экрану

**Критерий успеха:** картинка честно скрывается под Блокнотом, при перетаскивании окна CPU не превышает 2%.
**Если провалится:** пересматриваем раздел 3 ARCHITECTURE.md. Запасной вариант — `WS_EX_LAYERED` + `UpdateLayeredWindow` (медленнее) или отказ от выборочных слоёв в пользу двух режимов «поверх всего» / «на рабочем столе».

---

## M0 — фундамент (1–2 недели)

Скучно, но без этого всё остальное будет переписываться.

- [x] Репозиторий, воркспейс из шести крейтов, `rustfmt` + `clippy` в CI
- [x] GitHub Actions: сборка под Windows, clippy как ошибка, тесты
- [x] Логирование через `tracing`, файл лога в `%LOCALAPPDATA%\resticker\logs\`
- [x] Модель данных в `rst-core`: `Sticker`, `Transform`, `VisibilityRule`, `MonitorId`
- [x] Конфиг: чтение, запись, атомарность, `schema_version`, миграции-заглушка
- [x] Каркас окна настроек на Tauri 2 (пустые вкладки)
- [x] Иконка в трее с меню
- [x] Автозапуск через реестр, тихий старт
- [x] Манифест приложения с `PROCESS_PER_MONITOR_DPI_AWARE_V2`

**Результат:** программа запускается, висит в трее, открывает пустое окно настроек, читает и пишет конфиг.

---

## M1 — оверлей и первый стикер (2–3 недели)

- [ ] `rst-win32`: создание оверлей-окна на монитор, message pump на отдельном потоке
- [ ] `rst-render`: D3D11-девайс, композиционная цепочка, шейдер спрайта с premultiplied alpha
- [ ] Загрузка PNG/JPEG/WebP/BMP через `image`, приведение к premultiplied, загрузка в текстуру с мипмапами
- [ ] Отрисовка стикера по трансформации (позиция, размер, поворот, прозрачность)
- [ ] Событийный рендер: перерисовка только по требованию, ноль кадров в покое
- [ ] Добавление стикера из окна настроек, появление в центре основного монитора
- [ ] Сохранение и восстановление между запусками

**Результат:** можно добавить PNG, он висит поверх всего, переживает перезапуск. Клики проходят насквозь.
**Проверить здесь:** idle CPU действительно ноль.

---

## M2 — режим редактирования (3–4 недели)

Самая объёмная веха по количеству мелочей. Здесь рождается ощущение продукта.

- [ ] Глобальный хоткей через `RegisterHotKey`, обработка конфликтов
- [ ] Вход/выход из режима: снятие `WS_EX_TRANSPARENT`, забор фокуса, затемнение 50%
- [ ] Хит-тестинг с обратной аффинной трансформацией
- [ ] Рамка выделения + 8 ручек, зоны курсора
- [ ] Перемещение
- [ ] Ресайз за ручки, `Shift` — пропорции, `Alt` — от центра
- [ ] Поворот за угол, `Shift` — шаг 15°
- [ ] Магнит к краям и центрам монитора, `Ctrl` — отключить
- [ ] Ограничение «минимум 10% видно с каждой стороны»
- [ ] Мультивыделение: рамка, `Shift`+клик, `Ctrl+A`
- [ ] Undo/Redo, command stack
- [ ] Тулбар под стикером: ползунок прозрачности + числовое поле
- [ ] Кнопки тулбара: глаз, порядок выше/ниже, дублировать, удалить
- [ ] Диалог удаления с «Больше не спрашивать»
- [ ] Шахматка `#FF00FF`/`#000000` для скрытых стикеров
- [ ] Панель у курсора: загрузка файла, показать/скрыть всё, настройки, выход
- [ ] `Ctrl+V` из буфера обмена (PNG, `CF_DIBV5`, `CF_HDROP`)

**Результат:** продуктом уже можно пользоваться каждый день.

---

## M3 — мультимонитор (1–2 недели)

- [ ] Оверлей-окно на каждый монитор, корректная геометрия
- [ ] Логические координаты, привязка стикера к монитору по центру bbox
- [ ] Стабильный идентификатор монитора через device interface path
- [ ] Обработка `WM_DPICHANGED`, тест на смешанном DPI 100/150/200
- [ ] Перетаскивание стикера между мониторами
- [ ] `WM_DISPLAYCHANGE`: подключение, отключение, смена разрешения
- [ ] Сценарий отключения монитора: таймер 20 с, миграция на основной, поле `origin`, автовозврат
- [ ] Восстановление после потери устройства D3D (`DEVICE_REMOVED`)
- [ ] `WTS_SESSION_LOCK` / `UNLOCK`, обработка сна

**Результат:** ни один стикер не теряется ни в каком сценарии с мониторами.

---

## M4 — слои видимости (2–3 недели)

Технически рискованная часть, но благодаря спайку S0 основная неопределённость уже снята.

- [ ] Перечисление реальных окон с фильтром (cloaked, tool window, owner, ancestor)
- [ ] `DWMWA_EXTENDED_FRAME_BOUNDS` вместо `GetWindowRect`
- [ ] Инкрементальный кэш окон на WinEvent-хуках, дебаунс 16 мс
- [ ] Рендер маски перекрытия, скруглённые углы 8 px
- [ ] Шейдер стикера с `discard` по маске
- [ ] Быстрый путь: `allow_list == ALL` → маска не считается, хуки не ставятся
- [ ] Панель выбора окон: иконки, заголовки, группировка по процессу, live-обновление
- [ ] Кнопка-переключатель «Выбрать все» с двумя состояниями
- [ ] Пресет «только рабочий стол» = пустой список
- [ ] Сохранение целей по пути процесса для восстановления между запусками
- [ ] Отсечение полностью перекрытых стикеров из рендера
- [ ] Настройка «Скрывать от захвата экрана» (`WDA_EXCLUDEFROMCAPTURE`) с проверкой результата
- [ ] Настройка «Не перекрывать панель задач»

**Результат:** ключевая отличительная фича проекта работает.
**Проверить здесь:** CPU при активном перетаскивании чужого окна по экрану.

---

## M5 — анимация и видео (3–5 недель)

Самый непредсказуемый по срокам этап: FFmpeg всегда преподносит сюрпризы.

**M5a — GIF и анимированный WebP (~1 неделя)**
- [ ] Декодирование всех кадров в текстурный атлас, анимация через смену UV
- [ ] Собственные часы на стикер, планировщик ближайшего дедлайна
- [ ] Потоковый режим для очень длинных анимаций
- [ ] APNG

**M5b — видео, программный декод (~1,5 недели)**
- [ ] Сборка и линковка FFmpeg (LGPL shared, `--disable-gpl --disable-nonfree`)
- [ ] Демуксер, декодер, очередь на 3 кадра, пул потоков
- [ ] Луп через `av_seek_frame`
- [ ] Конверсия YUV → RGB на GPU в шейдере (не через `sws_scale` на CPU)
- [ ] Пауза декодирования для невидимых стикеров

**M5c — аппаратный декод (~1 неделя)**
- [ ] `d3d11va` hwaccel, кадр как `ID3D11Texture2D`, zero-copy рендер
- [ ] Определение поддержки, корректный fallback

**M5d — звук (~1 неделя)**
- [ ] Микшер на `cpal`/WASAPI, ресемплинг
- [ ] Аудиочасы как ведущие, синхронизация видео
- [ ] Громкость на стикер, глобальный mute + хоткей
- [ ] Приглушение невидимых стикеров

**M5e — прозрачное видео (~0,5 недели)**
- [ ] `yuva420p` (WebM/VP9), `yuva444p10le` (ProRes 4444), `qtrle`
- [ ] Принудительный программный декод для таких файлов + отметка в UI

**Результат:** OBS-сцена в реальном времени на рабочем столе.

---

## M6 — стикеры-окна (1–2 недели)

- [ ] Режим выбора окна с подсветкой под курсором
- [ ] `pin` / `unpin` через `SetWindowPos` + оконное свойство-маркер
- [ ] Гарантированное открепление при выходе, включая аварийный и `WM_ENDSESSION`
- [ ] Перемещение и ресайз в пределах правил окна
- [ ] `EVENT_OBJECT_DESTROY` → корректное удаление
- [ ] Обработка UIPI: понятное сообщение про права администратора
- [ ] Виртуальные рабочие столы через `IVirtualDesktopManager` с fallback

---

## M7 — пресеты (1 неделя)

- [ ] Сохранение и загрузка полной расстановки
- [ ] Вкладка «Пресеты» в настройках, переименование, удаление
- [ ] Быстрое переключение из трея и из панели редактирования
- [ ] Диалог недостающих элементов со списком и кнопкой «Загрузить остальное»
- [ ] Экспорт и импорт файла пресета с предупреждением о персональных данных внутри

---

## M8 — доводка и релиз 1.0 (2–3 недели)

- [ ] Первый запуск: короткий онбординг, показать хоткей
- [ ] Понятные тексты ошибок вместо кодов
- [ ] Инсталлятор (MSI или NSIS), подпись если будет сертификат
- [ ] Профилирование, устранение найденных горячих точек
- [ ] Прогон 24 часа с 25 стикерами, проверка утечек
- [ ] Достижение целевых показателей из SPEC раздел 13
- [ ] README со скриншотами и гифками, страница релиза
- [ ] Локализация: русский и английский

---

## Сводка по срокам

| Веха | Оценка |
|---|---|
| S0 спайк | 2–4 дня |
| M0 фундамент | 1–2 недели |
| M1 оверлей | 2–3 недели |
| M2 редактирование | 3–4 недели |
| M3 мультимонитор | 1–2 недели |
| M4 слои видимости | 2–3 недели |
| M5 видео и анимация | 3–5 недель |
| M6 стикеры-окна | 1–2 недели |
| M7 пресеты | 1 неделя |
| M8 релиз | 2–3 недели |
| **Итого** | **16–25 недель** |

Это 4–6 месяцев при указанной загрузке. Если сроки важнее полноты, есть очевидная точка выхода: **после M4 продукт уже полноценный и уникальный**, а M5 (видео) — самая дорогая веха. Релиз 0.5 после M4 — вполне рабочая стратегия.

---

## Отложено (после 1.0)

Изначально в задумке был встроенный редактор в духе Photoshop. Он вынесен из 1.0 сознательно: по трудозатратам это отдельный продукт, сравнимый со всем остальным вместе взятым, и его отсутствие никак не мешает основному сценарию.

**Инструменты рисования (условно 2.0)**
- Слои рисования, превращающиеся в стикеры
- Кисть, ластик как маска (обратимая), фигуры, линии, текст
- Векторное представление, чтобы масштабирование стикера не портило качество
- Эффекты: размытие, свечение, тень, цветокоррекция
- Поддержка нажима графического планшета
- Сохранение слоя как редактируемого проекта, а не плоского PNG

**Прочий бэклог**
- Прозрачность и поворот для стикеров-окон
- Цветная рамка вокруг закреплённого окна (как в PowerToys)
- Условная видимость: показывать набор стикеров только при запуске определённой программы
- Автопереключение пресетов по контексту
- Стикеры-ярлыки: клик открывает файл или ссылку
- Анимированные переходы при показе и скрытии
- Веб-контент как стикер (кусок страницы, виджет)
- Захват области экрана в стикер (живое зеркало окна)
- Плагины
