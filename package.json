{
  "name": "resticker",
  "private": true,
  "version": "0.1.0",
  "description": "resticker settings window (Tauri) — build tooling only, no runtime JS framework in M0",
  "scripts": {
    "test:i18n": "node crates/resticker/ui/i18n.check.js"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
