{
  "name": "resticker",
  "private": true,
  "version": "1.0.4",
  "description": "resticker settings window (Tauri) — build tooling only, no runtime JS framework in M0",
  "scripts": {
    "test:i18n": "node crates/resticker/ui/i18n.check.js",
    "test:hotkeys": "node --test crates/resticker/ui/hotkey_test.js",
    "test": "npm run test:i18n && npm run test:hotkeys"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
